import { Star } from '@/components/ui/icon';

export const StarIcon: React.FC<React.SVGAttributes<{}>> = (props) => {
  return (
    <Star aria-hidden fill="currentColor" strokeWidth={0} {...(props as any)} />
  );
};

export const StarIconNew: React.FC<React.SVGAttributes<{}>> = (props) => {
  return (
    <Star aria-hidden fill="currentColor" strokeWidth={0} {...(props as any)} />
  );
};
